{
  "name": "monea",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node --env-file-if-exists=.env.local dev-server.cjs",
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "check": "node --check app.js && node --check client/account.js && node --check server/api.cjs && node --check service-worker.js"
  },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.0",
    "stripe": "^18.5.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.7",
    "@playwright/test": "^1.55.0",
    "esbuild": "^0.25.9"
  }
}
